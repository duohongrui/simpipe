#' Summary
#'
#' @param data Cell properties of reference data
#' @param group Cell properties of simulated data
#' @param batch Gene properties of reference data
#' @param k Gene properties of simulated data
#' @param DEGs The number of CPU cores to use.
#' @param verbose The number of CPU cores to use.
#'
#'
#' @return A list
#' @export
# data_functionality_summary <- function(
#   data,
#   group = NULL,
#   batch = NULL,
#   k = NULL,
#   DEGs = NULL,
#   verbose = NULL
#
# ){
#
# }
# a <- powsimR::CELseq2_Gene_UMI_Counts
#
#
# estimate_output <- simpipe::estimate_parameters(ref_data = as.matrix(a),
#                                                 method = "Splat",
#                                                 seed = 10,
#                                                 verbose = TRUE,
#                                                 use_docker = FALSE)
#
# result <- simpipe::simulate_datasets(method = NULL,
#                                      other_prior = list(batchCells = 1000),
#                                      parameters = estimate_output,
#                                      seed = 10,
#                                      return_format = "list",
#                                      verbose = T,
#                                      use_docker = FALSE)


